rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for now to test functionality
    // TODO: Tighten security rules after testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}